# Known pre-existing test failures (behavior drift) — baseline for the CI gate
# (scripts/ci_test_gate.py). NEW failures outside this list fail CI. Regenerate
# with: python scripts/ci_test_gate.py --update
#
# Down from 42 to a single flaky timing test after the drift-reconciliation
# sweep. test_confirm_trade_success was rewritten for the LIVE-ONLY path and
# test_all_28_scenarios_pass now derives its thresholds from live CONFIG, so
# both pass and were removed from this baseline. Remaining:
#  - test_dedup_prevents_repeat: flaky (time-based ProactiveMonitor dedup).

tests/test_intent_and_monitor.py::TestProactiveMonitor::test_dedup_prevents_repeat
